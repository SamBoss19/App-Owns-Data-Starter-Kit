server {
    listen 5000;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page-app fallback: unknown routes return index.html so the
    # React Router can handle them client-side.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
